(provide 'vemv.project.emacs)

;; This file must exist but normally you'll never have to tweak it.

(setq vemv/project-type :elisp)

(setq vemv/project-root-dir (concat vemv-home "/.emacs.d/"))

(setq vemv/project-clojure-dir (concat vemv/project-root-dir "lib/non-submodules/"))
