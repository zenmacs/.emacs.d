#name: db
# --
(datomic.api/q
  '[:find $1
    :in $
    :where ]
  (-> (or com.stuartsierra.component.repl/system acceptance.support.fixtures/*running-system*)
      :config
      pepkey.components.config/database-url
      datomic.api/connect
      datomic.api/db))